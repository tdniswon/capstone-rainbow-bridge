<p id="notice"><%= notice %></p>

<p>
    <strong>Order description:</strong>
    <%= @order_type.order_description %>
</p>

<%= link_to 'Edit', rails_admin.edit_path(model_name: 'order_type', id: @order_type.id) %> |
<%= link_to 'Back', :back %>