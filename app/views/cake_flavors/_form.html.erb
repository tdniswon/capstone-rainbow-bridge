<%= form_with(model: cake_flavor, local: true) do |form| %>
  <% if cake_flavor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cake_flavor.errors.count, "error") %> prohibited this cake_flavor from being saved:</h2>

      <ul>
        <% cake_flavor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flavor_name %>
    <%= form.text_field :flavor_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
