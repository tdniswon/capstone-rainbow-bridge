<%= form_with(model: rental_item, local: true) do |form| %>
  <% if rental_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rental_item.errors.count, "error") %> prohibited this rental_item from being saved:</h2>

      <ul>
        <% rental_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rental_item_name %>
    <%= form.text_field :rental_item_name %>
  </div>

  <div class="field">
    <%= form.label :rental_item_description %>
    <%= form.text_field :rental_item_description %>
  </div>

  <div class="field">
    <%= form.label :rental_item_cost %>
    <%= form.text_field :rental_item_cost %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
