<%= form_with(model: filling, local: true) do |form| %>
  <% if filling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filling.errors.count, "error") %> prohibited this filling from being saved:</h2>

      <ul>
        <% filling.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filling_name %>
    <%= form.text_field :filling_name %>
  </div>

  <div class="field">
    <%= form.label :filling_description %>
    <%= form.text_field :filling_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
