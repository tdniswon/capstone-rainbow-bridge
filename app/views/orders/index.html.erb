<!-- Added by Tyler 1/31, For the Orders page that will contain the orders form.-->

<h1>Orders Page</h1>

<%= simple_form_for :test do |f| %>
    <%= f.input :first_name %>
    <%= f.input :last_name %>
    <%= f.input :order %> 
    <%= f.button :submit %>
<% end %>